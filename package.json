{
  "name": "unibot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/*.js && node --check test/*.js"
  },
  "engines": { "node": ">=22.5.0" }
}
