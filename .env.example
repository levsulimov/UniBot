# Token issued for the MAX bot. Never commit the real value.
MAX_BOT_TOKEN=replace-me
PORT=3000
DATABASE_PATH=./data/unibot.sqlite
# The official MAX Bot API endpoint. Change only for a test proxy.
MAX_API_BASE_URL=https://platform-api.max.ru
